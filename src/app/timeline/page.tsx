/**
 * Route stub. The page itself lives in `src/routes/timeline.tsx` and is rendered
 * by `RouteHost` (see `src/components/route-host.tsx`), which keeps it mounted
 * across tab switches — which is also what preserves the feed's scroll position.
 * This segment only has to exist so the URL resolves.
 */
export default function Page() {
  return null;
}
